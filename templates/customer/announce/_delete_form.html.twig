<form method="post" action="{{ path('customer_app_announce_delete', {'id': announce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ announce.id) }}">
    <button class="btn-danger mt-5">Delete</button>
</form>
