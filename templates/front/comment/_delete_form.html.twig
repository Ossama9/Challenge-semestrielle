<form method="post" action="{{ path('front_app_comment_delete', {'id': avi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.id) }}">
    <button class="btn">Delete</button>
</form>
