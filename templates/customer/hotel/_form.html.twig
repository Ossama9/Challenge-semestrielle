{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn-primary">{{ button_label|default('Save') }}</button>
    <a class="btn-secondary" href="{{ path('customer_app_hotel_show', {'id': hotel.id})}}">Retour</a>
{{ form_end(form) }}
